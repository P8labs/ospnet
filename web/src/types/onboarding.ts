export interface OnboardingTokenResponse {
  token: string
  expires_at: string
}

export interface OnboardingTokenRequest {
  // Currently no fields required, but structured for future extensibility
}
